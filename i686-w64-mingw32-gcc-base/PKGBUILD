# Maintainer: rubenvb
pkgname=i686-w64-mingw32-gcc-base
pkgver=4.6.2
pkgrel=1
pkgdesc="Cross GCC for i686-w64-mingw32 (Windows 32-bit)"
arch=('i686' 'x86_64')
url="http://gcc.gnu.org"
license=('GPL' 'LGPL' 'FDL' 'custom')
groups=
depends=('zlib')
makedepends=('i686-w64-mingw32-binutils' 'libmpc' 'cloog' 'ppl' 'gcc-ada')
#checkdepends=('dejagnu') # Windows executables could run on Arch through bin_mft and Wine
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=('!libtool' '!emptydirs')
source=(ftp://gcc.gnu.org/pub/gcc/releases/gcc-${pkgver}/gcc-core-${pkgver}.tar.bz2)
md5sums=('780f614ab18c7a9066dec6387d7490b2')

build() {
  cd "${srcdir}/gcc-core-${pkgver}"

  #do not install libiberty
  sed -i 's/install_to_$(INSTALL_DEST) //' libiberty/Makefile.in

  echo ${pkgver} > gcc/BASE-VER

  cd ${srcdir}
  mkdir -p gcc-build && cd gcc-build

  ../gcc-core-${pkgver}/configure --prefix=/usr \
      --enable-languages=c,lto \
      --enable-shared --enable-static \
      --enable-threads=win32 \
      --with-system-zlib --with-ppl --enable-cloog-backend=isl \
      --enable-lto \
      --disable-multilib --enable-checking=release
  make
}

package() {
}

